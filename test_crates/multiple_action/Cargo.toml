[package]
name = "multiple_action"
version = "0.1.0"
edition = "2024"

[dependencies]
unwinding = { path = "../../", features = ["panic", "panic-handler", "personality", "system-alloc"] }
libc = "0.2"
